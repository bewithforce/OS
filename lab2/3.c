#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>
/*
int main(int argc, char* argv[], char* envp[]){
    for(int i = 0; i < argc; i++){
        printf("%d argument of program is %s\n", i, argv[i]);
    }
    printf("\n");
    int i = 0;
    while(envp[i] != NULL){
        printf("%d argument of environment is %s\n", i, envp[i]);
        i++;
    }
}
*/