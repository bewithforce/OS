#include <cstdio>
#include <cstdlib>
#include <unistd.h>
/*
int main() {
    size_t size;
    size = write(0, "write to 0", 11);
    if(size != 11){
        printf("can\'t write to 0\n");
        exit(-1);
    }
    size = write(2, "write to 2", 11);
    if(size != 11){
        printf("can\'t write ato 2\n");
        exit(-1);
    }
    size = write(1, "write to 1", 11);
    if(size != 11){
        printf("can\'t write to 1\n");
        exit(-1);
    }
    return 0;
}
*/