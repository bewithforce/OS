#include <unistd.h>
#include <string>

using namespace std;
/*
int main(){
    auto i = 0;
    while(dup(1) >= 0){
        i++;
    }
    auto s = "max number of fds: " + to_string(i) + "\n";
    write(1, s.c_str(), s.length());
}
*/
